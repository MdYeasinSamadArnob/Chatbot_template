# Backend URL (used by Next.js API route proxy — server-side only)
BACKEND_URL=http://10.11.200.99:9001

# Admin KB access (server-side only — never exposed to browser bundle)
ADMIN_SECRET=Era-KB-Admin-2026-S3cr3t
NEXT_PUBLIC_ADMIN_ENABLED=true
