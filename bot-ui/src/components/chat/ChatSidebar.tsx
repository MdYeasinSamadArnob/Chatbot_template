// ChatSidebar is no longer used — replaced by ChatScreen for the full-screen webview.
// Kept as an empty stub to avoid breaking any residual imports.
export function ChatSidebar() {
  return null;
}
